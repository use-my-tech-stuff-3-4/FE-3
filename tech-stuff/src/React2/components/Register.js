import React, { useState } from 'react'; 
import axios from 'axios'; 

const Register = (props) => {

  const handleSubmit = (event) => {
  
  }

  return (
    <div>
      {/* < React 1 Register Form > */}
      {/* <button onClick={handleSubmit}>Create account</button> */}
    </div>
  )

}

export default Register;